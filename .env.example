DATABASE_URL=postgresql://postgres:1234@localhost:5432/ai_fitness_coach
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:5173

# Get a key at https://resend.com — free tier is enough for dev.
# EMAIL_FROM must be onboarding@resend.dev unless you've verified your own domain in Resend.
RESEND_API_KEY=
EMAIL_FROM=AI Fitness Coach <onboarding@resend.dev>

# Get a key at https://platform.openai.com — powers the AI Coach chat.
OPENAI_API_KEY=

# The only account (by email) that can see /admin.
ADMIN_EMAIL=
